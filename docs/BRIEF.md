# Swanipe — Full Project Brief

## PRD
**Problem**: Free RPCs are 1-2 seconds behind. Traders miss launches.
**Solution**: Build primary project (DeFi/AI) on RPC Fast endpoint. Cross-submit for infra credits. Integration = swap 1 URL.

**Core Features**: RPC Fast as primary infrastructure, latency comparison chart (RPC Fast vs free), WebSocket subscriptions.
**Difficulty**: 0/10 for cross-submit. **Time**: 5 minutes (change RPC URL). **Bolt-on to**: Yigent, Oblivion, or any Solana project.

---

## BUILD PLAN
Hour 0: Replace `https://api.devnet.solana.com` with RPC Fast endpoint in primary project. Add latency comparison chart. Done.

---

## SUBMISSION
**Demo**: Side-by-side latency chart showing RPC Fast vs free endpoint. "Our app uses RPC Fast for sub-100ms response times."

---

## SEED DATA
Latency benchmark data: RPC Fast p50=45ms vs free p50=1200ms.

---

## UI
Single latency comparison chart embedded in primary project's dashboard. RPC Fast logo in footer.
